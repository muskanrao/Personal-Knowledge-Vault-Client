import React from "react";

function NoteItems() {
    //post api for type:notes -> no save button -> when user switch to back it should automatically save and add the data.
    
}

export default NoteItems;
